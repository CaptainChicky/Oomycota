I made this because I am now forced to drive like 30 mins every day to and from work at various facilities including a jewish one (the bank) and a gus fring one (a chemistry lab). so see i normally play music during said drive using soundcloud, but its so fucking saturated with ads so what can i say? time to donwload all this offline and make my own

i love jews and walter white

**the file structure of this is as follows**
```
root/
  index.html            the player itself (mirrors soundcloud basically)
  style.css             css for the main player
  app.js                javascript for the player logic and UI 

  manifest.json         PWA manifest (makes it installable on ios)
  sw.js                 service worker (offline caching)
  icon.png              app icon for home screen and favicon etc
  tracks.json           [AUTOGENERATED] track and playlist data from the python manager

  music/                the actual music
  covers/               [AUTOGENERATED] cover art for playlist/tracks, named via hash

  oomycota_manager.py   instead of editing json directly this has a GUi to do it for you
```

So basically since this is fully static, there cant be any backend processing so you gotta process the audio files and playlists yourself manually. i'm using a json to store stuff, and manually editing that is meh, so there's the accompanying tkinter GUi editor python file you can use instead to do everything properly. the entire python GUi should be self explanatory (lets you edit track metadata, reorder tracks, assign cover images, create and manage playlists with emoji icons or cover art, and exports tracks.json to the site root), but in case someone has lower IQ than the status quo (i love you koishi) the main things of note are that:
1. on the top right, first thing you do is to open a folder. **you have to open ROOT directory folder**, not the actual music folder inside the root. the gui editor specifically looks for a `/music/` subdirectory within the current working directory.
2. then when you open the folder, you then must click "scan for mp3s" on the top middle of the gui with the magnifying glass icon, and this will populate the list of music with all files in `/music/` that have the extensions: '.mp3', '.m4a', '.webm', '.mp4'
3. the extract art button to the right of scan for mp3s will extract any art embedded in the music files and automatically put them into covers. you **DO NOT** edit anything in the covers folder they are automatically generated and named via hash.

The python file uses mutagen to read ID3 tags and extract embedded album art, so you need to `pip install mutagen`. That's really the only requirement though so no pip install requirements.txt type shit is needed :p... And i should note its a bit finicky like if you want to reset your tracks.json you either delete it, or like if you wanna reset a specific track you have to delete the entry and then re-load it using the gui.

Here is the overview of the main display page (keep in mind this is a static soundcloud replacement so ive been trying to get it to look/feel as close as possible):

The main screen shows your full track list. Each track displays cover art (or a generated gradient placeholder based on the title), title, artist, album, a favorite button, and duration. Clicking a track starts playback and builds a queue from all visible tracks starting at that position downwards. Playlist selection buttons at the top let you filter the track list: All, Favorites (auto-generated when you have any), and any custom playlists defined in tracks.json. Selecting a chip filters the view and scopes the queue. Playlists show their cover art image if one was assigned through the manager, falling back to the emoji icon. There's a heart button on every track, in the full player, and in the context menu. These are stored in localStorage and persist across sessions, and automatically generate the favorites playlist icon above for selection.

The queue is a slide-up panel accessed from the queue button on the player bar. It shows the current track with playback controls (play/pause, skip, progress bar) at the top, then "Up Next" with the real play order and per-track remove buttons, then "History" of already-played tracks greyed out. There's a "Clear" button to empty the queue. Right click (desktop) or long-press (mobile) any track anywhere for a context menu with "Play Next" (inserts at front of Up Next) and "Add to Queue" (appends to end).

Toggling shuffle physically reorders the Up Next list using the Fisher-Yates shuffle. If you open the queue panel, you see the shuffled order. Toggling off it will restore the original order, minus anything that already played. As for repeat, there's three modes that get cycled through: off, repeat all (loops the full queue), repeat one (loops current track), pretty standard etc.

The player bar is a persistent bottom bar with cover art, title/artist, prev/play/next controls, time display, shuffle/repeat/queue buttons, and volume slider (desktop). A thin progress bar sits on the top edge and is scrubbable by click or touch-drag. No waveform like soundcloud because that requires preprocessing or some server womp womp. If you tap the cover art or track info on the player bar, it opens a full-screen overlay with large album art, track info, a scrubable progress bar with timestamps, full playback controls, and favorite + queue buttons. Swipe down or click on a button (top left) to close. There's also an inline search bar in the header on desktop, full-screen overlay on mobile. Searches title, artist, and album fields as per usual. The search is searching from all tracks, not the currently displayed playlist. 

The main important thing is the media session API which allows lock screen and notification controls on Android and iOS: play/pause, skip forward/back, seek, and a progress bar. Shows track title, artist, album, and cover art. Works with headphone buttons and car Bluetooth controls. This was the main goal of the project along with having a functional playlist/queue lol in order to play stuff in the car.

The entire thing should be installable as a standalone app via "Add to Home Screen" (as a PWA). The service worker caches the HTML shell and tracks.json on install, then caches each MP3 and cover image on first play. The download button bulk-caches based on what you're viewing: all tracks, just favorites, or a specific playlist. Long-press (mobile) or right click (computer) the download button to clear the cache and start fresh. Persistent storage is requested on load so the browser won't auto-evict your cached music. After caching, a toast shows storage usage (e.g. "487MB used of 6144MB available").

The website saves the current track, playback position, volume, queue state (Up Next + History), shuffle/repeat modes, and active filter to localStorage every 5 seconds and on pause/tab close. When you reopen the page, everything is restored. Doesn't auto-play, just cues up where you left off.

On desktop, we have the following keyboard shortcuts: Space (play/pause), left/right arrows (skip), S (shuffle), R (repeat), Escape (close overlays). Do note this captures the keys fully, so if you're in the page and say want to do a browser shortcut like Ctrl+R, it would trigger the repeat toggle instead and you instead have to manually click the refresh button.